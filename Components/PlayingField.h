class PlayingField
{
    public:
        PlayingField(int width, int height);
        ~PlayingField(); 
        int height, width;      // size of the playing field
        int x,y;                 // the position of the playing field 

};